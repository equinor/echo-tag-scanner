const belowViewfinder = 0;
const viewfinder = 1;
const cameraControls = 2;
const overlays = 3;
const errorBoundary = 501;

export const zIndexes = {
  belowViewfinder,
  viewfinder,
  cameraControls,
  overlays,
  errorBoundary
};
